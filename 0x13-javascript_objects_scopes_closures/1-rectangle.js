#!/usr/bin/node

// Define a Rectangle class
class Rectangle {
  // Takes two arguments: width (w) and height (h)
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
